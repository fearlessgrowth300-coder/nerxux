# ---- Hostinger deploy scripts environment ----
# Copy this file to ".env.deploy" (same folder) and fill in real values.
# ".env.deploy" is gitignored — never commit it.

HOSTINGER_HOST=2.25.126.125
HOSTINGER_USER=root
# Used by deploy_hostinger_server.py (key-based auth, run after setup_hostinger.py)
HOSTINGER_SSH_KEY_PATH=C:\Users\UPCOMING\.ssh\id_ed25519
# Used by setup_hostinger.py (first-time password auth, to install the SSH key above)
HOSTINGER_ROOT_PASSWORD=
HOSTINGER_AUTHORIZED_PUBKEY=

CLIENT_ORIGINS=http://localhost:5173,https://nerxux.vercel.app,https://*.vercel.app,*
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
VAULT_ENCRYPTION_KEY=
RUNPOD_API_KEY=
RUNPOD_POD_ID=
HOSTINGER_OLLAMA_URL=http://127.0.0.1:11434
